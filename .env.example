# Database — same PostgreSQL as housing-pro-next
DATABASE_URL="postgres://housing:password@31.97.23.166:5437/housing?sslmode=disable"

# Server
PORT=3001
CORS_ORIGIN="https://housingpro-tau.vercel.app"

# Resend — https://resend.com/api-keys
RESEND_API_KEY="re_..."

# Redis (direct TCP — VPS internal network via Easypanel)
# Create a Redis service in Easypanel and use its internal hostname
REDIS_URL="redis://redis:6379"

# Prolife CRM — forward Housing PRO leads automatically
PROLIFE_API_URL="https://www.prolifemed.com.br"
PROLIFE_LEADS_SECRET="gerar-um-secret-forte-aqui"

# Ollama — required for semantic search (/search and /search/sync)
# Runs on the same VPS; use internal Easypanel hostname
OLLAMA_URL="http://ollama:11434"
